# LEGACY — kept for tests that need a DRC-free per-step rule
# (tests/test_env/test_aug_scale_reward.py, tests/test_env_config.py).
# The training recipes use the drc_* rules instead.
#
# Shaped reward: per_step potential-based shaping every step.
# r_t = Φ(s') - Φ(s) - step_penalty
# DRC disabled by setting drc_penalty: 0.0.

name: shaped
mode: per_step            # "terminal" | "per_step"

potential:
  completion_bonus: 5.0
  unconnected_penalty: 1.0
  wirelength_penalty: 0.0
  drc_penalty: 0.0
  step_penalty: 0.01
  truncation_mode: none    # "none" | "penalty_only" | "full"
