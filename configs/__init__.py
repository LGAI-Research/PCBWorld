"""Configuration tree for the KiCad RL environment.

Data only at this level (``defaults/``, ``datasets/``, ``drc/``, ``masking/``,
``reward/``, ``paths.yaml``, …); all importable code lives in
``configs.loader`` (YAML IO, typed schema, path resolver, CLI).
"""
